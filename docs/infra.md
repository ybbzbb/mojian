# Infra

> mojian 是**本地单机创作工具**，无服务器拓扑、无常驻服务、无网络组件。以下记录当前已知的静态环境事实；多数「部署参数」在执行器实现前为 TODO。

## 运行环境

- 形态：本地命令行工具 `mojian` + 后期本地桌面客户端（Tauri 2）
- 无服务端、无数据库服务进程、无网络监听端口
- 目标平台：TODO（拟 macOS / Linux；Windows 经 Tauri 后期支持）

## 存储位置

- **机器状态**：本地 SQLite 数据库文件 —— 路径与文件名 TODO（schema 定稿后确定）
- **创作内容**：项目本地 markdown 文件（圣经、大纲、骨架、正文、评审意见）—— 目录布局 TODO
- **素材库**：参考小说的结构化骨架 + 节奏统计，入库索引（书 → 块 → 骨架文件）—— 路径 TODO

## 组件配置

> 无常驻组件。执行器为一次性调用的 CLI 进程。

- 客观红线（字数区间、对话占比、最长叙述段、与计划偏差率）为可配置项 —— 默认值 TODO（#7 开放问题：采用实测经验值约 2300-3000 字/章、对话占比 15-50%，或首卷学习后定标）

## 常驻 cron / watchdog

无。

## 访问方式

无网络访问；纯本地文件系统访问。

## 部署参数

TODO —— 待执行器实现后确定（Rust 版本基线、Tauri 版本、SQLite 驱动等属技术选型，见 `docs/tech-design.md`）。
